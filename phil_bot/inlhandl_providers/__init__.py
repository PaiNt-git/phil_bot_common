'''
#Это RegEx паттерн для этого обработчика, только кнопка с алиасом совпадающим по этому паттерну будет обрабатываться
INLINE_PATTERN = 'test_'

#Если труе - то прерывает расспространение событий, не давая последжующим обработчикам обрабатывать сообщение...
STOP_PROPAGATE = True

'''
